# 复制为 .env 并填入（.env 已在 .gitignore）
# 用于 DeepSeek / OpenAI 兼容接口
DEEPSEEK_API_KEY=你的密钥

# 可选：如果终端访问 DeepSeek 超时，但浏览器/代理可用，可填本机代理端口
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890
